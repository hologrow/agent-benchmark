[project]
name = "benchmark-runner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.18.4",
    "openai>=2.31.0",
    "sqlalchemy>=2.0.49",
]

[dependency-groups]
dev = [
    "requests>=2.33.1",
]
